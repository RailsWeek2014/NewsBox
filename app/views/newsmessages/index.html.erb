<h2>Feeds</h2>
	<% #= form_for(@search) do |f| %>
	<%# = f.label :tags %>
	<%#= f.text_field :tags %>
	<%#= f.submit %>
	<%# end %>

	<%# @listener.each do |listelement|%>
	<%# listelement %>
	<ul>
  <%# byebug %>
  <% for entry in FeedEntry.all %>
  <li><a href=<%= entry.url %> ><%= entry.name %></a></li> 
  <% end %>
  </ul>

<p><%= link_to "Feed Liste", listeners_path %></p>