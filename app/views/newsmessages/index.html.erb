<h2>Feeds</h2>
	<% #= form_for(@search) do |f| %>
	<%# = f.label :tags %>
	<%#= f.text_field :tags %>
	<%#= f.submit %>
	<%# end %>

	<% @newsmessage.each do |message| %> 
	<div id="rssfeed">
	<img src=<%= message.image %> alt=<%= message.title %> >
	<div id="rsstext">
	<a href=<%= message.url %>><%= message.title %></a> <br />
	<%= message.comment %></div></div>
	<%end%>
	<br>
<p><%= link_to "Neuen Feed hinzufügen", new_newsmessage_path %></p>